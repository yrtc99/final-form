import React from 'react';
import Dashboard from './Dashboard';

// Re-export the Dashboard component as TeacherDashboard
const TeacherDashboard = () => {
  return <Dashboard />;
};

export default TeacherDashboard;
